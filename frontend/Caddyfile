:80 {
	root * /app/dist
	file_server

	# SPA fallback
	try_files {path} /index.html

	# Gzip
	encode gzip

	# Cache static assets
	header /assets/* Cache-Control "public, immutable, max-age=31536000"
}
